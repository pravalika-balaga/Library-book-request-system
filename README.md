# Library-book-request-system
Library Book Request System using ServiceNow that allows students to submit book requests with details. The system validates data, generates a unique request number, stores records, and notifies the librarian. Librarians can view and manage all requests efficiently.
